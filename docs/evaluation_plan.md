# Evaluation plan for bed demand tool

Grey-marked rows represent aspirational predictions — flows that assume operational targets (e.g. 4-hour ED targets) not matched by observed reality. Standard observed-vs-predicted evaluation is not valid for these. Combined predictions that include any aspirational component are themselves aspirational.

## Flow evaluation matrix

| Row | Flow | Level | What is being predicted? | How modelled | Nature of predictions | Nature of observations | How to evaluate |
|-----|------|-------|--------------------------|--------------|----------------------|----------------------|-----------------|
| 1 | Admissions via ED/SDEC | Patient | P(admission after ED) | ML Classifier (XGBoost) applied to real-time data on patients in the ED without bed requests | Probability | Whether admitted or not (binary) | AUC and AUPRC, Discrimination plots, Calibration plots, MADCAP plots |
| 2 | Admissions via ED/SDEC | Patient | P(admission to subspecialty if admitted) | Probabilistic mapping of consult sequence to subspecialty of admission | Vector of probabilities of admission to each subspecialty | Subspecialty of admission | Evaluated indirectly via row 3 |
| 3 | Admissions via ED/SDEC | Service | Number of admissions of patients currently in the ED | Aggregation of predictions based on product of 1 and 2 | Probability distribution | Number admitted (at some point) | MAE and MPE, Obs-exp histogram, EPUDD |
| **4a** | **Admissions via ED/SDEC** | **Patient** | **Probability of admission in prediction window (aspirational)** | **Aspirational curve** | **Probability** | **N/A** | ***Aspirational — no evaluation*** |
| 4b | Admissions via ED/SDEC | Patient | Probability of admission in prediction window (based on prior data) | Survival curve | Probability | Whether admitted in window | Compare survival curves of training and test sets |
| **5a** | **Admissions via ED/SDEC** | **Service** | **Number of beds needed in prediction window, if 4-hour targets are met, for patients currently in ED** | **Aggregation of predictions based on product of 1, 2 and 4a** | **Probability distribution** | **N/A** | ***Aspirational — no evaluation*** |
| 5b | Admissions via ED/SDEC | Service | Number of beds needed in prediction window, assuming past ED performance | Aggregation of predictions based on product of 1, 2 and 4b | Probability distribution | Number admitted in window | MAE and MPE, Obs-exp histogram, EPUDD |
| 6 | Admissions via ED/SDEC of patients yet-to-arrive | Service | Arrivals in prediction window of patients later admitted | Rate of arrivals via ED/SDEC front door of patients later admitted | Arrival rate in each 15-min segment of the prediction window | Observed arrival rates | Histogram of difference between cumulative arrival rates and observed numbers |
| **7a** | **Admissions via ED/SDEC of patients yet-to-arrive** | **Service** | **Arrivals in prediction window of patients later admitted, if 4-hour targets are met** | **Time-varying Poisson using arrival rates from 6, weighted by probability of admission in window from 4a** | **Probability distribution** | **N/A** | ***Aspirational — no evaluation*** |
| 7b | Admissions via ED/SDEC of patients yet-to-arrive | Service | Number of beds needed in prediction window, assuming past ED performance | Time-varying Poisson using arrival rates from 6, weighted by probability of admission in window from 4b | Probability distribution | Number admitted in prediction window | MAE and MPE, Obs-exp histogram, EPUDD |
| 8 | Emergency admissions not via ED | Service | Number of emergency patients arriving during prediction window, not via ED/SDEC | Time-varying Poisson derived from historical arrival rates | Probability distribution | Number admitted in prediction window | MAE and MPE, Obs-exp histogram, EPUDD |
| 9 | Elective admissions | Service | Number of elective patients arriving during prediction window | Time-varying Poisson derived from historical arrival rates | Probability distribution | Number admitted in prediction window | MAE and MPE, Obs-exp histogram, EPUDD |
| 10 | Discharge from service | Patient | P(discharge from service within prediction window) | ML Classifier (XGBoost) applied to real-time data on inpatients and ED patients with bed requests | Probability | Whether patient left service in prediction window (binary) | AUC and AUPRC, Discrimination plots, Calibration plots, MADCAP plots |
| 11 | Discharges from service among emergency patients | Service | Number of beds vacated during prediction window by emergency patients under the service at prediction time | Aggregation over all emergency patients of predictions from 10 | Probability distribution | Number who left service in prediction window | MAE and MPE, Obs-exp histogram, EPUDD |
| 12 | Discharges from service among elective patients | Service | As 11 for elective patients | As 11 for elective patients | As 11 | As 11 | As 11 |
| 13 | Destination after current service (transition matrix) | Service | P(discharge or going to service B \| patient is in service A) | Mapping of proportions going from each of N services to another service or discharge (N+1 possible destinations). Used for binomial thinning | Transition matrix based on training set period | Transition matrix based on test set period | Heatmap residuals (tbc) |
| 14 | Transfers into service from among emergency patients | Service | Number of current emergency inpatients transferring in during prediction window | For a target service, convolve across all source services the source departure PMF (11) thinned by the corresponding source→target transfer probability from 13 | — | — | — |
| 15 | Transfers into service from among elective patients | Service | As 14 for elective patients | As 14 for elective patients | — | — | — |
| **16** | **All incoming emergency, including transfers** | **Service** | **Number of emergency admissions in prediction window** | **Convolution of 5a, 7a, 8 and 14** | **Probability distribution** | **N/A** | ***Aspirational — no evaluation*** |
| 17 | All incoming elective, including transfers | Service | Number of elective admissions in prediction window | Convolution of 9 and 15 | Probability distribution | Number who arrived in service in prediction window | MAE and MPE, Obs-exp histogram, EPUDD |
| **18** | **Net flow emergency patients** | **Service** | **Net change in number of emergency patients in service in prediction window** | **Convolution of 11 and 16** | **Probability distribution** | **N/A** | ***Aspirational — no evaluation*** |
| 19 | Net flow elective patients | Service | Net change in number of elective patients in service in prediction window | Convolution of 12 and 17 | Probability distribution | Net change in number in service in prediction window | tbc |

## Evaluation methods: interpretation

This table maps each evaluation method to where its logic is implemented in `patientflow`, and summarises what a "good" versus "concerning" signal looks like.

| Evaluation method | Where logic is defined in code | What to look for (good) | What to look for (concerning) |
|---|---|---|---|
| AUC and AUPRC — including at cross-validation stage | `src/patientflow/train/classifiers.py` (`evaluate_predictions`, `evaluate_model`) | AUC/AUPRC are high enough to show strong class separation and remain consistently high across folds. These metrics are secondary to calibration/discrimination diagnostics because downstream bed-count predictions depend on the accuracy of probability values, not just ranking ability. | AUC/AUPRC are low, or vary substantially across folds, indicating weak or unreliable discrimination. |
| Discrimination plots — pre- and post-calibration to handle imbalanced classes | `src/patientflow/viz/estimated_probabilities.py` (`plot_estimated_probabilities`) | Negative cases cluster at low predicted probabilities and positive cases at higher probabilities, with limited overlap. After recalibration, a sparse or vacant extreme right tail can be expected and is not, by itself, a problem. | Overlap increases and separation deteriorates versus the same model at the same prediction time before recalibration, and this is accompanied by poorer calibration/MADCAP or downstream bias (obs-exp/EPUDD). |
| Calibration plots | `src/patientflow/viz/calibration.py` (`plot_calibration`). Use both quantile and uniform options. | Points are close to the diagonal on the uniform calibration plot across clinically relevant probability ranges; quantile view shows the same direction of fit. | Systematic deviation from the diagonal (especially in higher-probability bins) on the uniform plot, even if the quantile plot appears smoother. Use uniform as the primary view and quantile as a robustness check. |
| MADCAP plots | `src/patientflow/viz/madcap.py` (`plot_madcap`, `plot_madcap_by_group`) | Cumulative predicted and observed lines track each other; desirable bowing pattern for discrimination. | Persistent gap between lines (predicted below observed = under-prediction of beds). Group-specific MADCAP can reveal subgroup failures. |
| Arrival rate plots | — | Differences centred near zero with balanced mass above/below and tight spread. | Skew/bias (mostly above zero = under-prediction, mostly below = over-prediction) or wide spread. |
| MAE and MPE | `src/patientflow/evaluate.py` (`calculate_results`, `calc_mae_mpe`) | MAE and MPE are low in absolute terms, with error magnitudes consistent with acceptable operational tolerance. Pair with obs-exp histogram and EPUDD plots to detect bias/spread that summary metrics can hide. | MAE or MPE exceed acceptable limits or imply meaningful error. Interpret alongside plots to reveal bias, skew, or spread that summary metrics alone can miss. |
| Obs-exp histogram | `src/patientflow/viz/observed_against_expected.py` (`plot_deltas`) | Differences centred near zero with balanced mass above/below and tight spread. | Skew/bias (mostly above zero = under-prediction, mostly below = over-prediction) or wide spread. |
| EPUDD | `src/patientflow/viz/epudd.py` (`plot_epudd`, helpers including `_calculate_cdf_values`) | Observed CDF points track model CDF structure closely across thresholds. | Consistent offset patterns indicating calibration bias in distribution predictions. |
| Survival curve comparison (training vs test) | Plotting: `src/patientflow/viz/survival_curve.py` (`plot_admission_time_survival_curve`). Curve computation: `src/patientflow/calculate/survival_curve.py` (`calculate_survival_curve`) | Train/validation/test curves are similar (no major temporal drift). | Clear gaps between curves, suggesting process drift and potential mismatch if model uses older periods. |
| Histogram of difference between cumulative arrival rates and observed numbers | `src/patientflow/viz/observed_against_expected.py` (`plot_arrival_delta_single_instance`, `plot_arrival_deltas`) | Delta trajectories hover around zero and average delta remains near zero; histogram of final deltas centred near zero. | Persistent positive/negative drift (systematic under/over prediction of arrivals) and shifted final-delta histogram. |
| Charts or metrics for transfer matrix | tbc | — | — |
